---
import PostDetailPage from '../../components/pages/PostDetailPage.astro';

const { id: rawId } = Astro.params;
const postId = rawId!.length > 12 ? rawId!.slice(-12) : rawId!;
---
<PostDetailPage postId={postId} />
